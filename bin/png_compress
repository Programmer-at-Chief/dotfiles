#!/bin/bash

directory="${1:-.}"

if [[ ! -d "$directory" ]]; then
  echo "Error: '$directory' is not a valid directory."
  exit 1
fi

find "$directory" -type f \( -iname "*.png" \) | while IFS= read -r file; do
  pngquant "$file" --ext .png --force
done
